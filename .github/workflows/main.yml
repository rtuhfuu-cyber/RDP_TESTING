name: RDP_TIPSUNIX_OPTIMIZED

on:
  workflow_dispatch:

jobs:
  secure-rdp:
    runs-on: windows-latest
    timeout-minutes: 3600

    steps:
      - name: Configure Core RDP Settings
        run: |
          # Aktifkan Remote Desktop & Izinkan NLA Authentication
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -Value 1 -Force

          # Hapus & tambahkan kembali rule firewall port 3389
          Enable-NetFirewallRule -DisplayGroup "Remote Desktop" -ErrorAction SilentlyContinue
          netsh advfirewall firewall delete rule name="RDP-Tailscale" 2>$null
          netsh advfirewall firewall add rule name="RDP-Tailscale" dir=in action=allow protocol=TCP localport=3389

          # Restart service Remote Desktop
          Restart-Service -Name TermService -Force

      - name: Create RDP User with Secure Password
        run: |
          # Buat password acak yang aman dan kompatibel (tanpa karakter bermasalah seperti \, ", $, &)
          $charUpper = [char[]](65..90)
          $charLower = [char[]](97..122)
          $charNum   = [char[]](48..57)
          $charSpec  = [char[]]@('!', '@', '#', '$', '%', '*', '-', '_')
          
          $passArray = @()
          $passArray += $charUpper | Get-Random -Count 5
          $passArray += $charLower | Get-Random -Count 5
          $passArray += $charNum   | Get-Random -Count 4
          $passArray += $charSpec  | Get-Random -Count 2
          $password = -join ($passArray | Sort-Object { Get-Random })

          $securePass = ConvertTo-SecureString $password -AsPlainText -Force
          
          # Hapus user RDP lama jika ada, lalu buat user baru
          Remove-LocalUser -Name "RDP" -ErrorAction SilentlyContinue
          New-LocalUser -Name "RDP" -Password $securePass -AccountNeverExpires
          Set-LocalUser -Name "RDP" -PasswordNeverExpires $true
          
          Add-LocalGroupMember -Group "Administrators" -Member "RDP"
          Add-LocalGroupMember -Group "Remote Desktop Users" -Member "RDP"
          
          Add-Content -Path $env:GITHUB_ENV -Value "RDP_PASS=$password"


      - name: Install Tailscale (Latest Version)
        run: |
          # Mengunduh Tailscale versi installer terbaru
          $tsUrl = "https://pkgs.tailscale.com/stable/tailscale-setup-latest-amd64.msi"
          $installerPath = "$env:TEMP\tailscale.msi"
          
          Invoke-WebRequest -Uri $tsUrl -OutFile $installerPath
          Start-Process msiexec.exe -ArgumentList "/i", "`"$installerPath`"", "/quiet", "/norestart" -Wait
          Remove-Item $installerPath -Force

      - name: Establish Tailscale Connection
        run: |
          # Pastikan Service Tailscale berjalan
          Start-Service -Name "Tailscale" -ErrorAction SilentlyContinue
          Start-Sleep -Seconds 5

          # Hubungkan ke Tailscale menggunakan Auth Key
          & "$env:ProgramFiles\Tailscale\tailscale.exe" up --authkey=${{ secrets.TAILSCALE_AUTH_KEY }} --hostname=gh-runner-$env:GITHUB_RUN_ID
          
          $tsIP = $null
          $retries = 0
          while (-not $tsIP -and $retries -lt 15) {
              $tsIP = & "$env:ProgramFiles\Tailscale\tailscale.exe" ip -4
              Start-Sleep -Seconds 5
              $retries++
          }
          
          if (-not $tsIP) {
              Write-Error "Tailscale IP tidak didapatkan. Cek apakah TAILSCALE_AUTH_KEY di GitHub Secrets sudah benar dan valid."
              exit 1
          }
          echo "TAILSCALE_IP=$tsIP" >> $env:GITHUB_ENV

      - name: Verify RDP Accessibility
        run: |
          Write-Host "Tailscale IP: $env:TAILSCALE_IP"
          $testResult = Test-NetConnection -ComputerName $env:TAILSCALE_IP -Port 3389
          if (-not $testResult.TcpTestSucceeded) {
              Write-Error "Koneksi TCP ke Port RDP 3389 gagal."
              exit 1
          }
          Write-Host "Koneksi Port RDP Berhasil!"

      - name: Maintain Connection
        run: |
          Write-Host "`n=================== INFO RDP ==================="
          Write-Host "IP Address : $env:TAILSCALE_IP"
          Write-Host "Username   : RDP"
          Write-Host "Password   : $env:RDP_PASS"
          Write-Host "================================================"
          
          while ($true) {
              Write-Host "[$(Get-Date)] RDP Aktif..."
              Start-Sleep -Seconds 300
          }
